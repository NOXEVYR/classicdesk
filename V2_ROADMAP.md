# V2 小步路线

目标：让用户在清晰的系统状态和真实效果证据下调整桌面。以下是待做事项，不代表首版已经完成。

| 阶段 | 具体工作 | 验收条件 |
| --- | --- | --- |
| V2.1 界面统一 | 重整三页行高、间距、控件对齐；将当前系统、草稿和已保存方案分开显示；每项提供参数图与简短效果说明 | 100%/125%/150%/200% DPI、最小窗口、键盘 Tab 顺序均无裁切；每个控件有名称、单位、默认/修改/错误状态 |
| V2.2 可理解的能力状态 | 为每项标记计划、代码实现、隔离验证、实机验证、当前不支持；缺组件说明包含可执行的下一步 | 没有组件时不存在可成功应用的假按钮；预览不暗示实机；失败原因可复制且默认不含个人路径 |
| V2.3 可公开的增强包 | 完成 THIRD_PARTY.md 所列源、依赖许可与版本锁定材料；设计单独下载和校验流程 | 每个资产与对应源可追踪；组件包和前端分开校验；下载体积提前可见；损坏、错误版本、额外模块均拒绝 |
| V2.4 第一条真实垂直路径 | 先验收“任务栏开始/应用布局”，再分别增加图标尺寸、Explorer 工具栏、右键菜单；每条能力独立试验和回退 | 在可恢复的 Windows 测试环境记录前后效果、启用/关闭/恢复、应用程序与 Explorer 重启结果；每项都有已测构建、限制及证据 |
| V2.5 恢复体验 | 提供事务时间线、前后值、归属冲突、崩溃后只读检查和人工处理说明；重复修改先明确上次方案状态 | 进程 PID 重用、文件外部编辑、注册表修订变化、启动不确定、日志落盘失败均不覆盖或强停；新会话恢复和确认后再漂移分别验证 |
| V2.6 兼容与轻量化 | 测试 Windows 更新、多显示器、主题变化；分别测前端和增强引擎的启动时间、CPU 与工作集 | 发布可复现测量方法和实测数据；关闭前端后无自有托盘/后台轮询；不把共享运行时大小计入“零成本” |

每阶段先实现一个可演示的小闭环，再扩展参数。没有真实效果证据的功能继续标为未验收，不用静态图或 fake 测试数量替代系统行为验证。
