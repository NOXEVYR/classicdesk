# 0.11.15-dev：开机方案更新界面

- 增加已保存开机方案与当前编辑方案的完整对比、“更新开机方案”和“停用下次开机增强”。方案相同、检查失败、操作进行中或提交后不重复写入；取消管理员授权保留原配置。标准与最小尺寸的两张离屏渲染已查看。
- `StageLayout` 在新目录生成规则，保留原始恢复来源和上一安装；重新核对账户、回执、固定资产、模块执行范围及服务身份，并在受保护路径中串行协调暂存。只修改下次启动路径，不停止服务或热重载 Explorer。
- Core **86/86**、Host **98/98**、Frontend **83/83**、ReleaseBoundary **23/23**、ShellService **23/23**、ServiceUI **9/9**、Windows PowerShell 5.1 隔离安装流程 **5/5**，共 **327 项**通过。安装流程以真实文件、模拟 SCM/ACL/原生检查运行；这些检查不等于真实服务配置切换已验收。
- 真实 0.11.14 安装通过新前端的只读开机方案检查。修正 .NET 子进程继承 PowerShell 7 模块路径时 Windows PowerShell 5.1 无法加载 Security 模块的问题。设置程序构建 0 警告、0 错误，配套管理脚本与独立停用入口一起交付。
- 本机仅更新设置前端至 0.11.15；服务及原生组件仍为 0.11.14，本次未更改用户开机方案、停止服务或重启外壳。**新方案的真实管理员写入与重启效果尚待实测**；公开下载仍为 0.10.0-preview。
- 没有新增后台常驻进程、轮询或联网；更新时会读取校验文件并保留独立版本目录。报告和真实安装信息留在本地，不提交公共仓库。

# 0.11.14-dev：背景最大化窗口被小窗口遮挡时保留不透明

- 修复只按前台小窗口判透明的问题。事件触发后按叠放顺序查找本屏仍显示的最大化/全屏应用，并以它为颜色来源；最小化、隐藏、cloaked 和其他屏幕窗口不作为最大化背景。
- 原生编译及策略断言通过；ShellService **14/14**、ReleaseBoundary **23/23**、Host 定向 **5/5**，共 **42 项**通过。保留历史运行时摘要，旧版本恢复兼容未移除。
- 新增受保护的 `StageUpdate`：核对上一版服务身份和完整布局，在新目录验证文件后仅更改 SCM 的下次启动路径。已回读更新成功，原服务与 Explorer 的进程 ID 保持不变。设置页明确显示等待重启，未将“更新已暂存”写成“当前已生效”。
- 2026-09-21 用户再次重启后，新版服务自动运行，早于桌面 Explorer 约 3.1 秒；六项增强模块及开始菜单定位模块均来自新版服务目录。34 个固定文件摘要一致，动态背景初始化阶段为 3、主题根数量为 2；检查时设置前端和便携引擎未运行，未查到本次开机后的相关崩溃或服务错误。
- **2026-09-21 用户实测通过**：最大化浏览器后点击前方普通小窗口，任务栏保持不透明；将浏览器最小化且没有其他最大化窗口时，任务栏自动透明。此次确认覆盖上述两步，不扩大为首屏无跳变、注销或外壳重建验收。

# 历史记录 · 0.11.13-dev：开机服务已安装，冷启动实测待进行

2026-09-21 重启后补充：服务已自动运行，创建时间早于 Explorer 约 3.2 秒；六项 Explorer 增强模块及开始菜单定位模块从服务目录加载，动态背景初始化和两个主题根已建立。检查时前端及便携引擎未运行，固定代码/配置校验无差异，本次开机未查到相关崩溃。**服务自动启动已确认，首屏无跳变及用户操作结果仍待反馈**；下列安装时记录按原时间保留。详见 [服务开机检查](docs/SHELL_STARTUP.md)。

- 本机使用整机统一布局。受保护的 `ClassicDeskShell` 服务已安装，回读为自动、非延迟启动且当前停止；未启动服务、重启 Explorer 或自动重启 Windows。桌面设置入口已更新到 0.11.13。
- Core **86/86**、Host **98/98**、Frontend **83/83**、ReleaseBoundary **23/23**、ColdUpgrade **20/20**、ShellService **13/13**，共 **323 项通过**；前端生成 49 张离屏渲染。测试使用隔离数据，不注入真实外壳。
- 原生生命周期和崩溃阈值断言通过；非 SCM 启动、开发目录冒充受保护安装均被拒绝。同一固定工具链重复构建获得相同摘要。安装后 40 个文件逐项校验通过，代码和规则目录权限检查通过。
- 修正安装器在 Windows PowerShell 5.1 中读取中文路径时的 UTF-8 编码问题；原生 GUI 检查进程显式等待退出。问题出现时服务保持停用，修正并回读成功后才设为下次启动。
- 新前端在服务登记后锁定旧便携引擎的启用、恢复、接续和登录补加载入口。保留旧事务、用户方案及独立停用入口，服务模式的方案更新界面尚待完成。
- **尚未验证**：服务首次真实启动、桌面首屏即生效、注销/Explorer 重建、实际崩溃保护、服务模式资源占用和长期稳定性。本次同步源码及说明，不发布新的可下载增强包，不上传本机预备包、缓存或用户记录。

# 0.11.12-dev：动态透明实机确认

- 在旧引擎退出且外壳无残留组件后完成冷切换，沿用完整已应用方案。用户确认普通窗口、最大化、还原、最小化依次透明、不透明、透明、透明，无需额外点击桌面。
- 这次在登录后显式启动引擎，不能作为开机首屏验收。此前热重载崩溃问题仍未宣称解决。

# 历史记录 · 0.11.10-dev：外壳加载前置修正（当时未部署）

- 0.11.9 在重启后的干净 Explorer 中已实际启用，六项组件 Loaded，前台浏览器不透明底板有截图，用户确认点击桌面可透明；但最小化后仍需点桌面，自动切换未通过。
- 0.11.10 修正已失焦窗口最小化/隐藏事件的过滤，并在前台暂留任务栏时检查是否存在可见应用。原生模块改为允许 Shell_TrayWnd / Taskbar.View 尚未创建的早期初始化，通过创建/加载钩子补齐绑定；没有新增空闲轮询。
- 原生编译与策略断言通过，ReleaseBoundary **22/22**，独立 early-init 包 **20** 项资产、**13,602,266** 字节校验通过。此次没有重新声称实机自动切换或首屏效果通过，也未重复无关前端检查。
- 用户已确认接受外壳组件，要求任务栏第一次出现就有保存的布局。登录补应用脚本已撤销，当前桌面仍运行 0.11.9。系统级加载安装/恢复与冷启动验收尚未完成，见 [SHELL_STARTUP.md](docs/SHELL_STARTUP.md)。本轮没有安装服务、改动 Windows 外壳入口或再次重启 Explorer。

# 0.11.9-dev：已同步状态

- 新任务栏规则为应用前台不透明、桌面前台透明，不依赖窗口最大化；保留明暗配色、单次事件延迟和原有尺寸。设置页与运行状态文案同步，旧配置字段和旧运行包清单保留恢复兼容。
- 原生策略断言通过；本轮 Host 定向检查 **5/5**、Frontend **83/83**（49 张离屏渲染）、ReleaseBoundary **21/21**。独立测试包 **20** 项资产、**13,598,170** 字节通过内置摘要校验。此前完整隔离检查 285 项通过，本轮未重复全部检查。
- 当前同步源码、原生模块源码、资产清单和文档。公开下载继续为 0.10.0-preview 前端包，没有发布或自动启用增强引擎。测试报告、真实配置、恢复记录和本机运行目录不提交。
- **实机未通过**：0.11.8 经一次主动 Explorer 重启后背景组件仍未加载，同版组件重载期间发生 Explorer 崩溃并由系统自动恢复，StartMenuExperienceHost 也报告定位模块异常。0.11.9 只完成新规则实现与隔离验证，未解决加载/重载故障、未启用新包。新托盘间距、每次登录接续和完整替代 StartAllBack 均不能宣称已完成。详见 [任务栏验收](docs/TASKBAR_BEHAVIOR.md)。

# 0.11.1-dev：按需组合增强

- 增加任务栏布局、尺寸、资源管理器和右键菜单的独立选择；默认仍仅选择任务栏，非任务栏功能需用户在检查面板勾选。未勾选任务栏布局时保留原对齐值（包括不存在），未勾选尺寸时尺寸模块停用。
- Core **73/73**、Host **65/65**、Frontend **79/79**、ReleaseBoundary **18/18**，共 **235 项通过**。Host 新增全部 16 种选择映射、仅菜单/仅布局的启用恢复、跨宿主重建恢复、未修改对齐值的外部漂移阻断和旧方案序列化兼容检查。
- 前端新增选择失效、范围传递、原生样式禁选及方案开关保存/导入/重置测试。首轮发现开关挤占常用尺寸首屏，移入高级布局后通过；最终 47 张离屏截图，核对新增面板常规与最小窗口。图标和皮肤资源没有修改。
- 新字段默认不写入 JSON，保留旧方案序列化与恢复日志指纹；选择仅投影当前草稿，不覆盖保存的完整方案。全部使用隔离配置与模拟注册表/进程，无真实系统写入或增强启用。
- 开发构建位于 `artifacts/ClassicDesk-0.11.1-dev/app`，同级组件重新从固定资产准备为停用状态，17 个文件、11,059,674 字节通过内置清单校验。更新本地启动入口前保留旧版本与用户方案备份；公开下载仍为 0.10.0-preview。
- 没有新增后台扫描、计时器、服务或联网任务。整体资源对照与 Windows 实机效果尚未完成，不宣称已经替代 StartAllBack。报告留在 `artifacts/v011-features/`，不提交用户数据或机器路径。

# 前一阶段：本地任务栏运行准备

- 用户改为先补功能后，本地前端新增完整方案库：命名保存、更新、载入预览、撤销载入、复制、重命名、归档与恢复。只按页面或操作读写，名称不作为文件名，不新增常驻进程、监听或定时器。原始用户方案未迁移或改写。
- 本批定向回归：Frontend **76/76**、ReleaseBoundary **18/18** 通过；45 张离屏截图，查看常规及最小窗口的方案库布局。覆盖空库无写入、完整参数往返、未保存草稿恢复、归档保留、重名拒绝、外部修订冲突、损坏/未来版本保护以及导出不能覆盖方案库。标题版本改为读取程序集，避免开发版继续固定显示 0.10。
- 首次实机切换尝试：经用户同意，备份 StartAllBack 配置、Explorer 设置和已保存方案，通过 StartAllBack 3.9.25 的“高级 / 为当前用户禁用该程序”选项停用，再重启当前会话 Explorer。在 Windows 11 25H2、26200.9457 上，完整 `StartAllBackX64.dll` 与加载器仍在新 Explorer 进程中；设置页明确提示需要注销。未放宽冲突检查，未启用 Windhawk，也没有注销用户会话。
- 已撤销这次临时停用，回读确认 StartAllBack 配置与备份逐项一致、ClassicDesk 已保存方案字节不变、TaskbarAl 仍为 0、无 ClassicDesk 前端或 Windhawk 进程。此次只完成切换前置条件验证，实际效果和性能对比仍待用户停用旧插件并注销重新登录后进行。前端冲突提示补充了这一操作条件。
- 官方固定版本安装器与四个预编译模块已下载校验，仅留本地。解压后核对 17 个运行文件，合计 11,059,674 字节；资产清单与前端编译摘要一致。运行组件保持四模块停用、双安全模式开启，没有执行安装器、启动引擎或修改当前桌面。
- 增加离线准备脚本及 `--check-runtime` / `--inspect-system` 只读诊断。准备脚本的 8 项现场检查通过，包括拒绝覆盖既有目录、原文件字节不变、损坏模块在创建输出前被拒绝、准备后无引擎进程。
- 前端试用限制为任务栏布局与尺寸；四套方案都保留原始草稿和皮肤。资源管理器及右键菜单模块在试用配置中保持停用，面板明确说明范围。
- 本轮最终检查：Core **73/73**、Host **44/44**、Frontend **67/67**、ReleaseBoundary **18/18**，共 **202 项通过**。首次前端测试构建缺少原生面板源文件引用，修正测试项目后重跑前端与发布边界均通过；此前通过的底层检查未重复执行。43 张离屏截图，已查看新增试用面板。
- 真实机器只读检查确认 StartAllBack 已加载，前端控制器返回禁止启用；任务栏对齐与运行目录字节保持不变，没有创建增强引擎进程。此结果只证明冲突阻断，不代表真实增强效果已经验收。
- 已完成当前 Explorer 的 30 秒只读空闲采样，采样窗口完整。数据包含 Windows 与所有已加载扩展，仅作为后续对照的首份记录，不能当作 StartAllBack 独占开销或整机性能结论。
- 本地开发构建为 `artifacts/ClassicDesk-0.11.0-dev/app`，运行目录为其同级 `原生组件-未启用`。旧桌面入口和公开 0.10.0-preview 均未替换；未发布第三方组件或新的 GitHub 版本。实机切换、恢复和同条件资源对照仍待完成。
- 报告在 `artifacts/v011-runtime-checks/` 和 `artifacts/runtime-stage/`，包含本机信息，不上传。可复用步骤见[本地组件准备](docs/LOCAL_RUNTIME.md)。

# 前一阶段：启用与恢复可靠性（本地开发）

- 完整 `scripts/Test.ps1` 验收通过：Core **73/73**、Host **44/44**、Frontend **66/66**、ReleaseBoundary **9/9**，合计 **192 项通过**。Release 开发构建 0 警告、0 错误。
- 复现到原版文件替换失败；增加只针对 Windows 共享/锁错误和 1175 的有界重试。每次重新核对原目标修订、待提交内容和宿主条件；1176/1177 等不确定替换不重试，不增加整次事务、注册表写入或进程启停重试。
- 回归覆盖真实 Windows 文件读锁释放、重试次数上限、等待期间外部修改、临时文件内容变化及 Windows 继承时间戳行为。保留故障注入的原始错误，便于定位后续问题。
- 恢复末尾重新核对所有目标和原增强进程退出状态；新增“先恢复的目标后来又被修改”及“原本未修改的目标后来变化”检查，避免误报已恢复。
- 运行组件缺失时仍提示未完成恢复记录，保持原始记录和禁用状态；损坏记录仍拒绝继续。
- 用户确认优先 Windows 11，并要求不增加整体资源负担。已记录 [低占用验收方法](docs/PERFORMANCE.md)，加入一次性只读采样脚本；仅验证采样工具自身，未把脚本数据当作应用占用或插件对照结果。
- 以上为隔离数据、模拟注册表与进程的验证，没有启用真实增强引擎、切换现有插件或修改桌面；本阶段修复尚未发布为新的 GitHub 版本。完整报告留在 `artifacts/v011-regression/`，不上传机器路径和测试夹具。

# 0.10.0 布局与皮肤拆分验证

- 发布前按用户选择恢复 0.9.1 的紫色星光显示器应用图标，标题栏、窗口、EXE/ICO 使用同一图案；其他导航与预览图标保持不变。最终图标版本重新运行前端检查，66/66 通过。

- 前端隔离检查 **66/66 通过**，42 张离屏截图，覆盖六页常规/最小布局、四个布局入口、六款皮肤、原生对比、保存/导入导出与重置。验证换肤不改变布局参数、换布局保留皮肤，图标保持不变。
- 旧版星空方案仅在内存中迁移；图标尺寸、菜单行为等参数及文件修订均保留，保存前不写回磁盘。新增三款插画内嵌资源，无运行时下载。
- 发布边界检查 **4/4 通过**，缺组件不会调用真实系统宿主。Release 输出成功，版本 0.10.0-preview；本次按用户要求发布前端预览版，不启用真实系统改造。
- 扩展底层检查未全通过：当前 Core 两轮均 58/59，但失败项不同；Host 首轮 43/44、复查 34/44。使用同一基准提交原始源码独立构建对照，Core 59/59、Host 38/44。失败现象也出现在原始宿主源码中，尚未定位根因；不能据此宣布系统启用与恢复可靠性验收通过。此次未改动 ShellActivation、ShellActivationHost 或其测试。
- 完整本地报告保存在 artifacts/v010-*.json，属于隔离测试产物，不作为公开发布材料。
- 本地切换：先在旧版窗口保存未保存草稿并确认“已保存”，正常关闭后备份配置，再更新桌面快捷方式及无控制台启动器。实际运行进程版本 0.10.0-preview，窗口响应正常；历史版本保留。

# 0.9.1 星空图标改版验证

- 图标改为原创星空矢量系列，覆盖应用品牌、侧栏、预设缩略图及预览中的主要应用与文件图标。核对星空方案整页截图与应用图标，前端隔离检查 58/58 通过。
- Release 输出至独立 0.9.1 目录，ICO 覆盖 16–256 像素。保留旧版本，未更改方案格式或真实 Windows 图标。

# 0.9.0 本地风格改版验证

- 前端隔离检查 **58/58 通过**，生成 29 张离屏截图，核对五套风格、画廊、资源管理器和最小窗口。覆盖风格切换与预览同步、具体方案命名、导出/导入/保存往返、按当前风格重置、旧版文件兼容和非法风格保护。
- 发布边界检查 **4/4 通过**，缺组件时没有系统宿主调用或启用票据。
- Release 本地输出成功；插画作为 WPF 资源内嵌，无运行时联网下载。ICO 包含 16–256 像素尺寸。
- 方案格式新增可选 Appearance 字段；旧文件缺失该字段时采用 Win10 风格，原有参数保留。
- 新程序位于本地 0.9 独立目录，保留历史程序。仅更新方案编辑和预览，未发布 GitHub 版本、未修改真实 Windows 外观。多显示器与真实系统应用效果不在本次验收范围。
- 桌面快捷方式已指向 0.9 的无控制台启动器及新版图标；实际启动进程版本为 0.9.0-preview，窗口响应正常。原生窗口未进行完整鼠键交互验收。

# 0.6.0 公开预览版验证

本公开整理版从精简源码重新构建，0 警告、0 错误。测试项目不依赖开发者的已安装增强组件或旧输出目录。

| 检查组 | 结果 | 范围 |
| --- | --- | --- |
| Core | 59/59 | 启用/恢复状态机、归属、日志失败、冲突、跨会话恢复 |
| Host | 44/44 | 真实隔离文件、fake 注册表/进程、包校验、最终后态复核 |
| Frontend | 38/38 | WPF 离屏布局、草稿、保存/撤销、错误、过期异步结果 |
| ReleaseBoundary | 4/4 | 缺组件诚实提示，无系统宿主调用、无日志创建、无启用票据 |

合计 **145 项通过**。离屏测试还生成了 13 张参数示意截图；公开源码选用其中 3 张作为 README 界面预览，前端运行 ZIP 不附图片。没有显示真实应用窗口，没有启用增强引擎、修改真实注册表或停止用户进程。

可以在 Windows + .NET 8 SDK 下运行 `scripts/Test.ps1` 重现检查。生成的详细日志和夹具只留在运行者本地，不作为仓库公开材料。

尚未验收：实际 Shell 视觉效果、真实首次切换/恢复、多显示器、不同 Windows 构建、重启后行为和实测资源占用。进程身份验证及模拟结果均不能替代这些检查。
# 0.8.0 本地视觉改版验证

- 前端隔离检查 52 项通过，新增布局卡与下拉框的双向同步检查。
- 检查默认 1080×820、最小 740×550 以及原有尺寸的五页布局；生成 23 张离屏截图并检查任务栏、预设页和最小窗口资源管理器页。
- 重新绘制程序与导航矢量图标，生成 16–256 像素的多尺寸 ICO；Release 构建和本地输出成功。
- 本次不改变方案格式、保存逻辑和系统启用底层。程序发布到本地 0.8 目录，0.6 / 0.7 目录保留，桌面快捷方式继续使用无控制台启动器。

# 0.7.0 本地界面改版验证

本次仅更新方案编辑与预览，不发布增强组件，不修改真实 Windows 设置。

- Release 构建通过；前端隔离检查 51 项通过，覆盖预设、草稿导入导出、上一份恢复、错误文件保护、跨页状态、预览重绘与 1000×740 / 740×550 布局。
- 缺组件发布边界检查 4 项通过。
- 核心检查首轮 57 项通过、2 项失败；原版对照及未修改代码的复查均为 59 项通过。首轮异常未定位为确定原因，不据此宣称系统启用可靠性已完成验收。
- 0.7.0 原生窗口已启动且响应正常。用户通过 Esc 停止了窗口自动操作，后续未继续自动点击；原生交互的完整人工验收仍待进行。
- 下载时发现的 0.6.0 宿主重复启用/恢复测试问题不在本次修改范围内。旧版程序与发布包保留；桌面入口改为本地 0.7.0，无控制台启动方式保持。
